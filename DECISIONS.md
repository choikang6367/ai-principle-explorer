# 개발 결정 기록

## 2026-08-31 — 초기 구조

- 화면은 긴 스크롤 문서보다 한 화면씩 진행하는 경험으로 설계한다.
- 첫 화면에서 학습 목표를 짧게 소개하고, `탐험 시작` 뒤에 주제를 선택하게 한다.
- 핵심 진행 키는 `Enter`로 두고, 선택 목록에서는 방향키 이동을 지원한다. `Escape`는 시작 화면으로 돌아가는 보조 키다.
- 외부 생성형 AI API는 첫 버전에 사용하지 않는다. 실제 결과처럼 보이지 않도록 교육용 시뮬레이션임을 명시한다.
- 질문 데이터는 `src/data` 아래의 별도 구조로 관리할 수 있도록 타입을 먼저 만든다.
- 실제 상용 모델의 내부 Chain of Thought를 보여준다고 표현하지 않는다. 시각화는 원리를 쉽게 만든 교육용 모형으로 설명한다.
- 의존성은 React, React DOM, Vite, TypeScript로 최소화하고, 애니메이션은 CSS로 처리한다.
- 색상만으로 선택 상태를 전달하지 않도록 `aria-pressed`, `SELECTED` 라벨, 선택 패널을 함께 제공한다.

## 2026-09-01 — 첫 화면 검증 후 보완

- 화면 전환 직후 자동 포커스가 내부 스크롤 위치를 바꾸지 않도록 카드 자동 포커스를 제거하고, 단계가 바뀔 때 상단 위치를 초기화한다.
- 카드의 `Enter`와 `Space` 입력은 명시적으로 선택 이벤트를 실행하도록 연결해 키보드 진행을 안정적으로 보장한다.
- 데스크톱 기본 뷰와 390px 모바일 뷰에서 시작 화면, 카테고리 화면, 선택 피드백을 확인했다.

## 2026-09-01 — 첫 시나리오 완주 흐름

- 질문 콘텐츠는 `src/data/scenarios.ts`에서 카테고리와 분리해 관리하고, 토큰·참고 대상·후보 확률·후보별 결과를 한 시나리오에 함께 둔다. 이번 버전은 6개 카테고리 1개씩과 동물 카테고리 추가 1개, 총 7개 예제를 제공한다.
- 모든 시나리오는 같은 단계 컴포넌트(`ScenarioStages.tsx`)를 사용한다. 실제 모델 출력을 호출하거나 내부 추론을 공개하지 않고, 미리 준비한 교육용 후보와 단순화된 확률로 원리를 설명한다.
- 후보 확률이 가장 높다는 사실과 과학적·생활 속 정답은 구분한다. 비교 화면에서 후보별 결과와 설명을 함께 보여 주어 학생이 문맥과 사실을 다시 확인하도록 한다.
- 진행 버튼은 브라우저의 기본 버튼 동작에만 의존하지 않고 Enter/Space 활성화를 명시적으로 처리한다. 선택 목록은 `aria-pressed`와 선택 라벨을 함께 사용하며, 방향키로 이동할 수 있다.
- 작은 화면에서는 단계 자체는 한 장면으로 유지하되 결과 정보가 세로로 쌓일 수 있다. 390px에서 가로 넘침이 없도록 검증한다.

## 2026-09-01 — 콘텐츠 검증과 진행 상태 복원

- 질문 콘텐츠는 한 번에 대량으로 만들지 않고 카테고리별 1개씩 추가해 이번 버전에서 총 12개로 확장한다. 각 후보 확률은 100이 되도록 자동 검사한다.
- 별도 의존성을 추가하지 않고 Node의 TypeScript 제거 실행을 사용해 `npm test`에서 시나리오의 ID·참조·확률·필수 설명을 검사한다.
- 새로고침 복원은 민감한 정보가 아닌 현재 단계와 선택 ID만 버전이 붙은 로컬 저장소에 기록한다. 저장값이 없거나 손상되면 시작 화면 또는 유효한 이전 단계로 안전하게 되돌린다.
- 사용자가 `처음부터 다시`를 선택하면 저장된 진행 상태를 지우고, 저장소를 사용할 수 없는 환경에서도 메모리 안의 체험은 계속 진행되도록 한다.

## 2026-09-01 — 콘텐츠 18개 확장과 저장 복원 회귀 검증

- 질문을 한 번에 대량으로 만들지 않고 각 카테고리에 1개씩 추가해 총 18개(카테고리별 3개)로 확장한다. 새 질문도 기존과 같은 토큰·참고 단서·후보·분기 결과 구조를 사용한다.
- 새 질문은 새의 둥지, 지구의 계절, 그림자, 팝콘, 수업 메모, 거울의 반사를 주제로 삼고, 초등학교 6학년 눈높이의 설명과 단순화된 교육용 확률을 유지한다.
- 저장 복원 회귀는 별도 의존성 없이 메모리 기반 `localStorage` 모형으로 malformed JSON, 알 수 없는 버전·단계·카테고리·시나리오·참고 토큰·후보 ID를 검사하고 `npm test`에 포함한다.
- Node에서 저장 모듈을 직접 실행할 수 있도록 해당 데이터 import에 `.ts` 확장자를 명시하고 TypeScript의 `allowImportingTsExtensions`를 활성화한다. 브라우저 번들 동작은 유지한다.

## 변경 기록 규칙

결정을 바꿀 때 기존 항목을 지우지 않고 날짜와 변경 이유를 아래에 추가한다.

## 2026-09-01 — 콘텐츠 24개 확장

- 장기 콘텐츠 확장은 한 번에 대량 작성하지 않고 카테고리별 1개씩 추가해 총 24개(카테고리별 4개)로 진행한다.
- 새 시나리오는 벌과 꽃, 별빛의 반짝임, 소금의 용해, 요구르트 발효, 함께 공부하기, 추운 날 입김을 주제로 하며, 기존 공통 구조와 단순화된 교육용 확률을 사용한다.
- 과학·생활 설명에서는 먹이와 꽃가루받이, 대기와 빛, 입자와 용해, 발효와 상함, 협력과 대신하기, 수증기와 물방울을 구분해 초등학교 6학년이 오해하기 쉬운 지점을 함께 설명한다.

## 2026-09-01 — 콘텐츠 30개 확장

- 장기 콘텐츠 확장은 이번에도 카테고리별 1개씩 추가해 총 30개(카테고리별 5개)로 진행한다.
- 새 시나리오는 물고기 아가미, 우주에서 소리가 전달되는 조건, 비눗방울의 표면 장력, 탄산음료의 이산화 탄소, 학교 규칙, 냉장고 문을 열 때의 열 이동을 주제로 한다.
- 각 후보는 정답을 자동으로 보증하지 않는 단순화된 교육용 확률로 제시하고, 소리와 진공, 기체와 기포, 물질의 이동과 사라짐처럼 학생이 혼동하기 쉬운 개념을 결과 설명에서 구분한다.

## 2026-09-01 — 콘텐츠 36개 확장

- 카테고리별 1개씩 추가하는 작은 묶음 원칙을 유지해 총 36개(카테고리별 6개)로 확장한다.
- 새 시나리오는 개미의 냄새 신호, 달 표면의 발자국, 금속의 열전도, 아이스크림의 녹음, 도서관의 조용한 환경, 자동문의 센서를 주제로 한다.
- 생물의 행동·우주 환경·열의 이동·상태 변화·공동생활·센서 작동을 다루되, 학생이 원인과 결과를 혼동하지 않도록 대체 후보의 결과 설명에서 개념을 명시적으로 구분한다.

## 2026-09-01 — 콘텐츠 42개 확장

- 카테고리별 1개씩 추가하는 작은 묶음 원칙을 유지해 총 42개(카테고리별 7개)로 확장한다.
- 새 시나리오는 거북이 등딱지의 보호, 지구의 자전과 낮·밤, 젖은 종이의 섬유 결합, 삶은 달걀의 단백질 변화, 실험 결과 기록, 바람과 증발을 주제로 한다.
- 보호 기능과 집의 비유, 자전과 공전·일식, 젖음과 종이 섬유, 열과 단백질, 기록과 자동 정답, 바람과 물의 증발을 결과 설명에서 구분해 초등학교 6학년 수준의 원인·결과 연결을 돕는다.
- 후보 확률은 정답을 보증하는 실제 모델의 수치가 아니라 비교 활동을 위한 단순화된 교육용 확률로 유지한다.

## 2026-09-01 — 콘텐츠 48개 확장

- 카테고리별 1개씩 추가하는 작은 묶음 원칙을 유지해 총 48개(카테고리별 8개)로 확장한다.
- 새 시나리오는 펭귄의 무리 생활과 체온 보존, 우주에서 본 지구의 파란색, 철의 녹스는 변화, 침 속 효소와 밥의 단맛, 발표 전 연습, 보온병의 단열을 주제로 한다.
- 함께 모이기와 공격, 바다·대기와 물감, 녹과 단순한 색 변화, 녹말 분해와 설탕 첨가, 연습과 완벽함, 열 이동 감소와 열 생성의 차이를 결과 설명에서 구분한다.
- 후보 확률은 실제 모델의 내부 계산이나 정답 보증이 아니라, 문맥에 따른 후보 비교를 위한 단순화된 교육용 수치로 유지한다.

## 2026-09-01 — 기본 흐름 우선 활성화

- 질문과 주제 콘텐츠는 나중에 추가한다는 최신 요구에 따라, 현재 화면에는 동물 주제와 대표 질문 `강아지는 왜 꼬리를 흔들까요?` 하나만 활성화한다.
- 기존 48개 시나리오와 6개 주제 정의는 `scenarioCatalog`·`categoryCatalog`에 보존하고, 별도의 활성 ID 목록으로 노출 범위를 제한한다. 나중에 ID를 추가하는 방식으로 확장한다.
- 활성 데이터가 없는 주제가 질문 화면에서 빈 상태가 되지 않도록 현재는 동물 주제만 선택 화면에 노출한다. 이는 기본 흐름을 먼저 완성하기 위한 임시 범위다.
- 콘텐츠 활성 범위가 바뀌어 이전 저장값이 숨겨진 질문을 가리킬 수 있으므로 진행 상태 버전을 2로 올려 이전 저장 상태를 안전하게 초기화한다.

## 2026-09-01 — 작은 Transformer 계산 예제 추가

- 실제 모델이나 외부 API를 연결하지 않고 `src/transformer/engine.ts`에 고정 임베딩·위치 정보·Q/K/V·두 개 Head·Causal Mask·Residual·Feed Forward·Logit·Softmax를 포함한 한 블록 계산 예제를 둔다. 모든 입력과 가중치는 결정적이며 유한 수치만 반환하도록 보호한다.
- 토큰화 화면의 `Transformer 계산 자세히 보기`는 선택형 6단계 패널로 구현한다. 토큰과 임베딩, Q/K/V, Attention 점수, Causal Mask, Head와 Feed Forward, 다음 토큰 확률을 순서대로 보여 주어 기존 한 장면 흐름을 방해하지 않게 한다.
- 패널의 확률은 `작은 Transformer 계산 예제의 확률`로 별도 표기하고 기존 시나리오의 후보 확률·학생/AI 비교 분기는 변경하지 않는다. 후보의 첫 토큰 연결은 설명용 매핑일 뿐 실제 GPT 출력이나 정답 보장이 아니다.
- Attention 수치는 모델의 생각·중요도·Chain of Thought를 공개하는 것으로 표현하지 않는다. 상단 안내와 기존 단계 안내에서 교육용 계산 모형이라는 한계를 함께 알린다.
- 모달은 Enter/Space, Escape, 이전 버튼, 방향키 탭 이동, 포커스 복귀를 지원하고 390px에서 가로 넘침이 없도록 한다. `npm test`에는 행 합·Causal Mask·유한 수치·결정성·후보 매핑 검사를 추가한다.

## 2026-09-01 — 코드 최적화와 안전성 검증

- 토큰화 화면에서 고정 입력으로 계산하는 Transformer 결과를 `useMemo`로 재사용해 패널을 열고 닫을 때 불필요한 계산을 줄인다. 기능·화면 흐름은 바꾸지 않는다.
- 로컬 저장소는 4,096자보다 큰 진행 데이터를 읽거나 쓰지 않고, JSON 오류·저장소 차단·private browsing 예외가 나도 메모리 안의 체험을 중단하지 않는다. 회귀 검사에는 oversized payload와 저장소 예외를 포함한다.
- 외부 리소스·API·난수·unsafe DOM 삽입을 사용하지 않는 현재 구조를 유지한다. `index.html`에는 same-origin 중심 CSP와 `no-referrer` 정책을 추가하고, `validate-safety.mjs`에서 위험 sink와 정책의 존재를 자동 검사한다.
- `npm audit --omit=dev --audit-level=moderate` 결과 알려진 취약점은 0건이다. 기본 및 390px 브라우저에서 CSP 적용 후 전체 흐름, 대체 분기, 새로고침 복원, 패널, 가로 넘침, 콘솔 error/warn을 다시 확인한다.
- CSP의 완전한 운영 적용은 호스팅 서버의 응답 헤더 설정에 의존하므로 공개 배포 전에 서버 설정을 별도로 확인한다.

## 2026-09-01 — 어린이용 문장 끊기와 AI 토큰화 비교

- 기존 단계 순서를 바꾸지 않고 토큰화 화면 안에 `TokenSplitPractice`를 추가한다. 아이가 공백을 제거한 글자 조각 사이의 경계를 눌러 직접 덩어리를 만들고, 선택 결과는 즉시 화면에 보여 준다.
- 비교 화면은 `내가 끊은 결과`, `아이 눈높이로 보기`(낱말 단위), `일반적인 AI 토큰화 예시`(단순화된 subword)를 나란히 보여 준다. 직접 나눈 방식이 한 가지 정답이라고 채점하지 않는다.
- 일반적인 AI 토큰화 예시는 현재 활성 질문의 원문을 보존하는 고정 로컬 데이터로만 둔다. 특정 모델의 정석 결과나 실제 GPT의 내부 생각·Chain of Thought로 표현하지 않고, 모델과 언어에 따라 달라질 수 있음을 안내한다.
- 경계 버튼에는 `aria-pressed`와 상태 라벨을 제공하고 Enter/Space, 다시 해보기, 기존 Transformer 패널, Escape, 모바일 레이아웃을 유지한다. 시나리오 검사에서 비교 조각이 원문을 바꾸지 않는지도 확인한다.

## 2026-09-01 — Transformer를 기본 단계 흐름에 통합

- Transformer는 토큰화 화면에서 따로 띄우는 모달이 아니라 토큰화 다음의 독립적인 일반 단계로 배치한다. 토큰화의 `Transformer 이해하기` 버튼으로 진입하고, 마지막 설명에서 `참고하기` 단계로 넘어가도록 진행 상태·Escape·새로고침 복원을 연결한다.
- 기존 계산 예제는 유지하되 화면 설명의 진입 장벽을 낮춘다. 6단계마다 레고 블록, 숫자 이름표, 손전등, 미래 가리개, 여러 탐정, 다음 조각 비유와 한 줄 공식을 함께 제공한다. 수치와 확률은 계속 고정된 교육용 모형이며 실제 모델의 생각이나 Chain of Thought가 아니다.
- Transformer 단계의 탭과 이전/다음 설명 버튼은 `aria-selected`, `aria-controls`, `aria-labelledby`, `aria-live`와 Enter/Space·방향키를 사용한다. 모바일에서는 탭을 3열로 줄이고 내용은 세로로 쌓아 390px 가로 넘침이 없도록 한다.
- 모달 전용 JSX와 CSS를 제거해 일반 단계 컴포넌트와 walkthrough 스타일만 남긴다. `npm test`, 프로덕션 빌드, 전체 완주·새로고침 복원, 390px 레이아웃, 브라우저 error/warn 부재를 다시 확인한다.

## 2026-09-01 — 150개 질문과 유형별 탐색

- objective에 정의된 동물·사람·일상·과학·학교 질문 150개를 `src/data/questionBank.ts`에 원문으로 보존한다. 각 카테고리는 지식형·상상형·선택·의견형을 10개씩 제공한다.
- 질문 화면은 유형 필터를 사용해 한 번에 10개만 보여 준다. 질문 카드와 선택 패널에 유형을 함께 표시해 답변 방식의 차이를 먼저 인지하게 한다.
- 질문 원문을 공통 시나리오 factory로 변환해 모든 질문에 토큰화, 참고 단서, 후보 확률, 선택 결과를 연결한다. 지식형·상상형·선택·의견형은 서로 다른 후보 문장과 설명을 사용한다.
- 후보 답변은 외부 AI의 실제 출력이나 사실 보증이 아니라, AI가 문맥에 따라 답변 방향을 고르는 교육용 시뮬레이션이다. Transformer 계산과 AI 토큰화 예시도 기존의 결정적 로컬 모형을 유지한다.
- 기존 6개 카테고리·48개 비활성 카탈로그와 충돌하지 않도록 새 5개 카테고리와 질문 ID를 기준으로 데이터를 정리하고, 진행 상태 버전을 3으로 올린다.
- 시나리오 검증은 총 150개, 카테고리별 30개, 유형별 10개, 후보 확률 합 100, 토큰 참조·원문 보존을 검사한다.

## 2026-09-01 — 학습부터 출력 검사까지 전체 AI 과정 연결

- 기존의 토큰화·Attention·후보 비교만 이어지던 흐름에 `미리 배우기 → 반복 생성 → 답변 검사` 단계를 넣어, 질문을 받기 전 학습 아이디어부터 답변을 내보내기 전 점검까지 한 번에 따라가도록 한다.
- `미리 배우기` 화면에서는 예시 문장 보기 → 다음 말 예상 → 실제 답과 비교 → 안쪽 숫자 조절의 순환을 6학년 눈높이로 설명한다. 브라우저에서 실제 대규모 학습을 실행하는 것으로 오해하지 않도록 교육용 설명임을 명시한다.
- 아이가 직접 만든 토큰 묶음은 다음 Transformer 계산의 실제 입력으로 사용하고, Attention에서 고른 단서는 마지막 조각의 참고 점수에 반영한다. 선택 전후 후보 확률을 다시 계산해 조작이 장식으로 끝나지 않게 한다.
- Transformer 교육용 한 블록에는 임베딩·위치 정보·Q/K/V·Causal Mask·두 개 Head·Residual·Feed Forward·Layer Normalization·Logit·Softmax를 포함한다. 실제 모델의 여러 블록·전체 어휘·학습 가중치를 재현하지 않는다는 한계를 함께 안내한다.
- 선택한 후보의 교육용 답변 재료를 한 조각씩 다시 계산하고 뒤에 붙이는 반복 생성 화면을 둔다. 실제 모델의 전체 어휘를 샘플링하지 않으며, 이 모형에서는 가장 높은 후보를 고르고 준비된 재료의 끝 또는 길이 제한에서 멈춘다.
- 출력 직전에는 빈 답변, 질문 적합성, 위험 표현, 사실형 질문의 근거 확인을 점검한다. 이는 안전 보증이나 실시간 검색 기반 사실 검증이 아니라, 답변을 그대로 믿지 말고 사람이 확인하는 습관을 연습하는 장치다.
- 이 확장으로 진행 상태 저장 버전을 4로 올리고, 사용자 토큰화 결과도 유효한 시나리오와 원문을 확인한 뒤에만 복원한다.

## 2026-09-01 — 브라우저 회귀 점검과 상호작용 복원 보완

- 단계가 바뀌면 새 화면의 제목에 프로그램 포커스를 보내고, 제목은 Tab 순서를 어지럽히지 않도록 `tabIndex=-1`로 둔다. 답변 전송 직후에는 답변 영역에 포커스를 보내 반복 Enter가 다음 단계를 건너뛰지 않게 한다.
- 전역 Enter는 단계 전환 직후의 짧은 중복 입력을 한 번 무시해 빠른 클릭·Enter 반복으로 다음 화면까지 연속 진행되지 않게 한다. 정상적인 키보드 진행은 짧은 안정화 구간 뒤 유지한다.
- 질문 입력창에서도 Enter/NumpadEnter로 같은 전송 동작을 실행하고, 토큰화 초기화 직후의 다음 버튼은 최신 토큰 묶음을 사용하도록 한다.
- 기존 클리핑 목적의 `overflow: hidden`은 `clip`으로 바꾸어 sticky 헤더가 실제 문서 스크롤을 따라 고정되게 하고, 320px 경계의 최소 폭 제약은 세로 스크롤바로 생기는 가로 스크롤을 만들지 않도록 해제한다. 모바일 토큰 경계 버튼은 최소 24×32px로 유지한다.
- 답변 전송 상태를 새로고침 후 복원하기 위해 진행 상태 버전을 6으로 올리고, 버전 5 데이터는 답변 상태를 만들어내지 않는 방식으로 안전하게 읽는다. 진행 상태 회귀 검사에 이 마이그레이션과 답변 복원을 포함한다.
